import { render as $ra } from './Counter.tsx';
$elements['a'] = $ra({"count":1});
import { render as $rb } from './Counter.tsx';
$elements['b'] = $rb({"count":3});
import { render as $rc } from '../Timer.tsx';
$elements['c'] = $rc({});
